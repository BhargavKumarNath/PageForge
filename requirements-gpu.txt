# PageForge — Full dependencies for local GPU development
# Install with: pip install -r requirements-gpu.txt

# Core
torch>=2.0
transformers>=5.0
cupy-cuda12x>=14.0
typer>=0.12
rich>=13.0
pydantic>=2.0
toml>=0.10

# Rust extension (build from source via maturin)
maturin>=1.3.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
